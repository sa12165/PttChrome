# PttChrome（增強版）

[![Deploy to GitHub Pages](../../actions/workflows/deploy.yml/badge.svg?branch=dev)](../../actions/workflows/deploy.yml)
[![CodeQL](../../actions/workflows/dynamic/github-code-scanning/codeql/badge.svg)](../../actions/workflows/dynamic/github-code-scanning/codeql)
[![Dependabot Updates](../../actions/workflows/dynamic/dependabot/dependabot-updates/badge.svg)](../../actions/workflows/dynamic/dependabot/dependabot-updates)

在瀏覽器上直接連 PTT 的網頁版終端機，fork 自 [robertabcd/PttChrome](https://github.com/robertabcd/PttChrome)，並加上一系列閱讀與使用體驗的增強功能。

---

## ✨ 功能特色

> 以下是本版本相較原版新增的功能，多數可在「偏好設定」中開關。

- **📖 好讀模式** — 文章自動翻頁、整篇連續捲動閱讀，像看網頁一樣順。（重構並修復了許多顯示問題；可自訂「跳到文末並切回原生模式」的熱鍵，預設為 `F8`，**再按一次即切回好讀並從文章開頭重新載入**；切回原生後用 `[` `]` 等鍵跳到另一篇文章時也會自動恢復好讀；按 `h` 說明、`o` 選項、`/` 搜尋、`;` 指定頁數等 pmore 功能鍵在好讀模式下也能正常使用，會直接顯示原生選單。）
- **📜 文章列表好讀模式（實驗性）** — 外觀近似原生列表（24 行畫面、行首 ● 游標），**黑名單命中的文章整列被隱藏、由前後文章補滿畫面**；翻頁／移動游標純本地零延遲，背景自動預讀前後頁；按 Enter 直接開啟選取的文章（含置底文）；**滑鼠左鍵單擊標題欄即開啟該篇**（滑鼠移過去時該列會亮起來；點日期或作者欄不會誤開）；滾輪＝上下翻頁；數字跳號用**網頁式輸入框**收參（畫面不閃原生）；其餘按鍵（`/` 搜尋、`v` 標已讀、`[` `]` `=` 同標題跳文、`Ctrl-P` 發文等）**單按即自動同步游標並切回原生模式執行**；之後停留在原生模式操作（避免畫面來回切換閃動），開啟文章或離開看板再回列表時自動恢復好讀。已知限制：僅支援 24 列標準終端機大小。
- **🖼️ 自動開圖／影片預覽** — 文章裡的圖片、影片連結自動顯示預覽，支援各大圖床直連、imgur 相簿、Twitter、YouTube、Twitch 等。**tenor 的 GIF 分享連結（`tenor.com/xxxxx.gif`）也會自動開圖**：這種網址其實是網頁不是圖檔，過去只會顯示載入失敗，現在會直接播放該 GIF（自動循環、無聲，改用體積約四分之一的影片檔播放）；此項需要下方的「imgur 圖片載入代理」保持開啟（同一個服務代為解析），關閉時 tenor 連結就單純不顯示預覽。圖片載入失敗時會自動重試，全部失敗才顯示可點重試的提示（不再靜默變空白，方便分辨「載入失敗」與「還在讀取」）。**點任一張自動開圖的圖，整篇圖片就一起放大到視窗寬度（高度等比拉長）方便看細節，再點一下縮回小圖；換文章或退出再進會回到預設小圖。**
- **📑 翻譯漫畫圖文並排** — 好讀模式偵測到「圖片連結＋下方翻譯文字」重複結構的文章（常見於翻譯漫畫文）時，右下角出現「圖文並排」浮動按鈕；點一下把每張圖與其翻譯合併成左圖右文兩欄，**翻譯欄會跟著捲動固定在視野內**，看長條漫畫不用再上下滾動對照。翻譯欄寬度會依最寬的翻譯行自動調整（文字不換行）。「上圖下文」與「上文下圖」兩種排版都支援：按鈕三段循環——第一下用「上圖下文」配對、再點改「上文下圖」配對、第三下還原；跟點圖放大一樣，退出文章或換文章就自動恢復預設。
- **🤖 圖文並排的裝置端 AI 校正（實驗性，需 Chrome）** — 翻譯常被空行切成好幾段，純規則只會把「緊貼圖片的那一段」搬進右欄，剩下的翻譯還是留在下面。開啟後右下角多一顆「AI 校正配對」按鈕，用 **Chrome 內建的裝置端 AI**（Gemini Nano，**完全在本機跑、不會把文章傳到任何伺服器**）判斷「圖片後面（前面）的哪幾段還在講這張圖」，把整段翻譯一起搬進右欄。規則結果會先畫出來，AI 算完再逐張補正，不會卡畫面；AI 答不出來就維持原本的規則結果，內容零遺失、再按一次即還原。需在「設定 → AI」先開啟「啟用裝置端 AI」（會開始下載模型，數 GB、只下載一次），再勾選這項功能；其他瀏覽器或裝置不符需求時整頁選項都會反灰、按鈕不會出現，行為與沒有這個功能時完全相同。
- **🔢 推文樓層編號** — 好讀模式下幫推文標上樓層號，跨頁也連續準確。
- **🧵 連續同作者推文自動合併** — 好讀模式下，同一人連續發的多則推文合併成一塊，去掉一再重複的帳號與時間，讀起來像一則完整留言：**一則一行**，第 2 則起自動對齊第一則的內容起點；**作者只顯示在第一則、時間只顯示在最後一則**（位置與配色比照原生，可以正常選取複製）；樓層徽章只標該段第一則。推文裡的圖片一樣自動開圖，且跟在**該則連結的下面**（跟文章內文一致）。只合併「連續」同作者，跨作者各自分開；可在「設定 → 增強功能」關閉。
- **🌟 高亮原PO推文／點選推文者** — 自動高亮原PO本人的推文；點任一推文列，會高亮該推文者在本篇的所有推文（再點一次取消）。
- **🚫 黑名單** — 隱藏指定帳號的推文。看板列表中該帳號的發文：原生模式顯示為「（本文已被黑名單）」（比照被刪除文的樣式，不再反黑）；文章列表好讀模式則整列隱藏。
- **🏷️ 標題黑名單** — 在看板文章列表中，過濾標題含有指定關鍵字的文章（一行一個關鍵字，不分大小寫）：原生模式顯示為「（本文已被黑名單）」，好讀模式整列隱藏。
- **🖱️ 黑名單右鍵快速新增** — 在看板列表對作者欄右鍵可直接把該作者加入黑名單（已在黑名單則反灰提示）；對標題欄右鍵會跳出輸入框、自動帶入該篇完整標題供編輯後加入標題黑名單；在文章內對推文者 id 右鍵也可直接加入。不用再開設定手動輸入。
- **🔍 快速搜尋（可自訂）** — 選取畫面上的文字後按右鍵，選單直接列出搜尋項目（不再是子選單），點一下用新分頁開啟。內建「Google 搜尋」「pixiv 使用者」「pixiv 圖片」三項，其中 pixiv 兩項**只有在選到的是純數字時才會出現**（pixiv 的使用者／作品 id 就是數字），選到中文時不會來亂。可在「設定 → 快速搜尋」停用內建項目，或自行新增任意搜尋網站（網址用 `%s` 代表選取的文字，並可設定「任意文字／純數字」的適用條件）。
- **🖼️ 推文／發文可以貼圖（把圖拖進來就上傳）** — PTT 本身沒有上傳圖片的功能，只能自己先傳圖床再貼網址。現在把圖片**直接拖進瀏覽器視窗**（或截圖後 Ctrl+V、或右鍵選單「上傳圖片」挑檔案）就會自動上傳到 [urusai](https://urusai.cc/) 圖床，傳完把圖片的**直連網址**填進你正在打的推文列或文章編輯器（送不送出仍由你決定）；因為填的是直連網址，本站與其他看板 client 都會自動開圖。**一次可以拖多張**：依序上傳，最後一起插入、以空白分隔。如果拖的當下人不在推文列或編輯器（例如還停在文章列表），**不會亂送字**，改成把網址複製到剪貼簿並提示你。傳過的圖會留一份**上傳紀錄**（右鍵選單「圖片上傳紀錄」開啟），可隨時挑一張再插入、複製網址或開啟該圖的刪除連結，不必重傳。單檔上限 50MB、只收圖片檔。預設為匿名上傳；想讓圖片歸在自己的 urusai 帳號下（之後可自行管理或刪除），可在「偏好設定 → 本機設定」填入 urusai 的存取憑證（該憑證只存在這台裝置，不會雲端同步，也不會寫進設定匯出檔）。整個功能可在「偏好設定 → 增強功能」關閉。
- **💬 長推文一鍵發送** — PTT 一則推文有長度上限（大約 20 幾個中文字，看板記 IP 的話更短），想講一段話得自己算字數、切成好幾則慢慢推。現在在文章畫面按右鍵選「長推文一鍵發送」，跳出輸入框讓你一次把整段話打完，程式會**自動依上限分段**（優先在標點或空白處斷、不切壞中文字）並**一則一則依序推出去**。推／噓／→ 可以選（PTT 本身的規則是 90 秒內連續推文會自動變成 `→`，所以通常只有第一則吃得到你選的類型）。送出期間畫面會蓋上進度視窗，顯示「第幾則／共幾則」，**撞到 PTT 的推文冷卻就自動等待並倒數**，等完繼續推；隨時可以按「取消」停掉還沒送出的部分（已經送出去的推文無法收回），剩下沒推完的內容會複製到剪貼簿。PTT 顯示不出來的字（emoji 等）會在輸入時就提示並自動略過。超過 20 則會先問一次再送。可在「偏好設定 → 一般 → 右鍵選單」關閉。
- **🔗 自動修復斷掉的連結** — 作者貼錯、或中間被空白截斷的網址，會在原文下方補上一行可點的修復版連結（好讀模式）。**連被推文寬度切成兩則的網址也會接回去**：PTT 推文輸入欄有長度上限，貼長網址會被硬切成上下兩則（`https://i.imgur.c` ＋ `om/Pn3XurX.jpeg`），在「連續同作者推文合併」的區塊下方補上接好的完整連結（一樣可點＋自動開圖），原文不改寫。有網址路徑的（`example.com /badpath.jpg`）一律修；至於「只靠一個空白才看得出是網址、又沒有路徑」的候選（`www . a .com`），因為和英文句子的句號長得一模一樣（`Call of Duty. It does not.` 會被誤看成 `Duty.It`，`.it` 是義大利網域），**預設不修**，需要時可在「設定 → AI」開啟下一項的裝置端複核，由 AI 判斷後放行。
- **🌐 沒寫 `https://` 的網址也自動連結** — 作者只打 `indiegametw.com`、`eaigc.filtergame.com` 這種裸網域時，**原位**直接變成可點連結（不另外加行，好讀與原生模式都有）。內建判斷會跳過「只是在文中被提到」的網域（`※ 發信站` 那類系統行、被括號包住的 `(ptt.cc)`、電子郵件的網域部分）。另可在「設定 → AI」開啟**裝置端 AI 複核**（Chrome 內建 AI，需先用同一頁的「啟用裝置端 AI」下載模型），它同時管上面那項修復功能：判斷句子裡的網域是「作者要你去看的網站」還是「單純提到的名字」（**減少**誤判的連結），以及被空白打斷的那串到底是不是網址（**放行**真的斷掉的網址）。關閉或不支援時，兩邊都退回不需要 AI 的保守結果。
- **🌏 自動連結支援中文網址** — 像 `https://zh.wikipedia.org/wiki/戈黛娃夫人` 這種路徑帶中文的網址，整段（含中文）都會做成可點連結，不再斷在中文字前面；遇到全形標點（。，、等）自動判定為句子結束、不會把後面的中文內文誤吃進連結。
- **𝕏 @帳號自動連結 X/Twitter** — 文章內文與推文裡的 `@帳號`，自動做成連到 `x.com` 的橘色連結。
- **🔖 文章代碼（AID）一鍵跳文** — 好讀模式中，內文與推文裡的 `#文章代碼`（如 `#1gIeu-3A (Android)`）自動做成連結；點一下自動切換到該看板並開啟該篇文章（沒寫看板就用目前文章所在的看板）。過程中顯示原生畫面並暫時鎖住輸入，失敗會提示並停在原生畫面。
- **↩️ 跳文後可返回原文** — AID 跳過去之後，畫面左下角會出現「← 返回」鈕（也可按 F9，快捷鍵可在設定裡改），一鍵回到跳轉前那篇文章，**連原本讀到的位置一起還原**；連續跳好幾篇可以一層一層退回去。回不去的情況（例如原文已被刪除、或原本是站內信）不會亂跳，會停在看板列表並說明原因。
- **🔗 文章分享連結（deep link）** — 在文章裡按 **F2**（或右鍵選單「複製本篇文章連結」）複製一組像 `https://<站台網址>/#Gossiping/M.1783270974.A.0CA.html` 的連結，貼到 LINE／Discord／記事本都行。別人（或你自己）點開就會**自動跳到那篇文章**：還沒登入的話會先記著，等登入完成再跳。連結格式就是把 PTT 網頁版網址（`https://www.ptt.cc/bbs/看板名/M.xxx.A.xxx.html`）的後兩段接在 `#` 後面，所以手上有文章網址就能自己複製貼上組出來；以前那種 `#看板名/文章代碼` 的舊連結也照樣能開。**多數情況下複製是瞬間完成、畫面完全不動**（直接讀文章裡的「※ 文章網址」那行換算），只有讀不到那行時才會退回舊做法（畫面會閃一下後回到原本的位置）。若你當時正在看別篇文章，跳過去之後一樣會有「← 返回」鈕可以回來。**搭配瀏覽器套件，連 `https://www.ptt.cc/bbs/…` 的原始文章網址都能自動轉來本站開啟**（見下方「讓 PTT 文章網址直接在本站開啟」）。
- **🔗 右鍵複製任一篇的文章代碼／分享連結** — 對內文或推文裡的 `#文章代碼`（如 `#1gIeu-3A (Android)`），以及文章裡貼的 `https://www.ptt.cc/bbs/…` 文章網址按右鍵，選單會多出「複製文章代碼 (AID)」與「複製文章分享連結」兩項。前者複製成 `#1gIeu-3A (Android)` 這種帶看板的寫法（貼回 PTT 或貼給別人都找得到），後者複製成可以直接點開的本站連結。（另外修好一個老問題：以前對文章代碼按右鍵會被誤當成一般網址，「複製連結網址」只會複製到一個 `#`，而且「複製本篇文章連結」等選項會整組消失。）
- **👀 複製前先看到會複製什麼** — 右鍵選單裡幾個「複製」長得太像，常常搞不清楚按下去到底會拿到什麼。現在每個複製選項下方會多一行灰字，直接顯示**實際會被複製的內容**：連結網址、文章代碼、文章分享連結，以及「複製本篇文章連結」（在畫面上讀得到「※ 文章網址」那行時）。網址太長時**從中間省略、保留結尾**（看得到 `.jpg` 這種關鍵字尾），選單也放寬了寬度。
- **🧹 右鍵選單變乾淨了** — 「輸入小幫手」（打 ANSI 色碼／符號／表情）與「Live 文小幫手」（定時自動更新推文）改為**預設不顯示**，需要的人可在「偏好設定 → 一般 → 右鍵選單」各自打開。
- **🌐 網址列會跟著你正在看的文章變動** — 讀文章時瀏覽器網址列自動變成該篇的分享連結，直接加書籤或複製網址列都是那一篇；離開文章回到列表就變回站台網址。不會多出一堆「上一頁」紀錄。長文要滾到內文末尾（出現「※ 文章網址」那行）才會補上，因為本站堅持不為了填網址列而讓畫面跳動。
- **🪟 點連結時盡量沿用原本的分頁** — 點外部連結時，如果你已經有一個登入好的 PTT 分頁，會直接交給那個分頁去跳，新開的分頁只會顯示「已在原本的分頁開啟」（也可以選擇改在新分頁開）。把本站**安裝成應用程式**（Chrome／Edge 網址列的安裝鈕）效果最好：點連結會直接切到既有視窗，連新分頁都不會開 —— 不過 Chrome 預設不會自動攔截連結，**第一次**點連結仍會開分頁並在網址列顯示「在應用程式中開啟」，點它並選「總是允許」之後才會生效。
- **🔔 分頁接手連結時會通知你** — 承上，連結被交給另一個分頁之後，那個分頁會主動出聲讓你找得到它：**分頁標題閃爍**、發一則**系統通知**（點通知就會直接切到該分頁），切回去之後畫面上還會留一則「已從外部連結接手 #文章代碼」的提示。系統通知需要授權，第一次在「偏好設定 → 一般」勾選這個選項時瀏覽器才會詢問；沒有授權（或被封鎖）也不影響標題閃爍與畫面提示。可在同一處關閉。
- **🎁 Steamgifts 抽獎代碼自動連結** — Steam 板 [抽獎] 文裡獨立成列的 5 碼 giveaway 代碼（如 `jQtf0`），自動做成連到 `steamgifts.com/giveaway/代碼/` 的可點連結（網站會自動導向完整網址）；只在文章有提到 Steamgifts 時才啟用，避免誤判一般文字。
- **🔐 記憶帳密／自動登入** — 開啟網頁時自動登入；密碼存在瀏覽器的密碼管理員，不會上傳雲端（詳見下方說明）。
- **🔑 兩階段驗證 (2FA) 自動填入** — 帳號有開 PTT 兩階段驗證時，可存入 TOTP 密鑰，登入時自動算出 6 位驗證碼填入；也可以留空，只自動填帳密、驗證碼自己輸入。
- **☁️ 偏好設定雲端同步** — 用 Google 登入後，設定會同步到雲端，換瀏覽器或裝置都不會遺失（PTT 密碼絕不上傳）。在「偏好設定 → 設定備份」分頁。
- **💾 設定匯出／匯入** — 在「偏好設定 → 設定備份」分頁可把目前所有設定存成一個 JSON 檔，換瀏覽器或重灌後再匯入還原，不必登入任何帳號。匯出檔**不含** PTT 帳號、密碼與 2FA 密鑰（也不含「上班模式」這類本機專屬設定），可以安心保存。匯入會立即生效，備份檔裡沒有的項目會回到預設值。
- **⚡ 透過 Proxy 連線** — 內建 Proxy 連線開關，免裝任何擴充即可繞過連線限制（詳見下方）。
- **🖼️ imgur 圖片載入代理（預設開啟）** — imgur 的 CDN 把台灣流量導到美國，同一張圖偶爾會卡住 9～24 秒。開啟後 imgur 圖片改由本專案的 Cloudflare Worker 轉發並快取，可消除這種卡住的情況（**一般情況下的載入速度不會變快**）。該服務會看到你的 IP 與所讀取的圖片位址，但**不留存任何紀錄**；代理失效時會自動退回直連 imgur，影片（mp4）一律直連不經過代理。可在「偏好設定 → 連線」關閉，或改填自己架的位址（留空即用內建的，刪空就回到預設；自架見 `proxy/imgur-worker`）。同一個服務也負責解析 tenor 的 GIF 分享連結（見上方「自動開圖」），關閉後 tenor 連結不會顯示預覽。
- **🌓 暗色／淺色主題** — 設定、選單等介面全面採用現代化元件，預設為暗色主題；可在「偏好設定 → 介面 → 主題」切換淺色／暗色／跟隨系統。
- **▏游標不重複（預設開啟）** — 文章列表、選單這類畫面 PTT 自己就會畫一個 `>` 游標，此時不再另外顯示閃爍游標，畫面乾淨許多；輸入框、編輯器等 PTT 沒畫游標的地方，閃爍游標照舊顯示，不會找不到打字位置。可在「偏好設定 → 介面」關閉。
- **🖱️ 滑鼠操作重新設計** — 所有滑鼠設定集中到「偏好設定 → 滑鼠」獨立分頁，**一個總開關就能把整組（含中鍵、滾輪）一次關掉**（關掉後文章裡的連結、圖片照樣可以點）。文章裡把滑鼠移到畫面最左邊會亮出一條提示帶，點一下就退出文章 —— 點到連結或圖片時一律以它們優先，不會誤退。文章列表改成**只有點在標題欄才會開文**，不再因為點到日期或作者欄而誤觸（可用「防誤觸模式」關掉這個限制，見下方）。滾輪固定為上下翻頁（文章好讀模式維持瀏覽器捲動）。中鍵可設為貼上或左方向鍵。
- **⌨️ 畫面上的功能鍵可以直接點（預設開啟）** — PTT 頂端與底部那排提示（`[←]離開` `[→]閱讀` `[Ctrl-P]發表文章` `[d]刪除` `[h]說明`，以及文章底部的 `(y)回應` `(X)推文` `(^X)轉錄` `(b)進板畫面`）現在是**可以點的按鈕**，點一下就等於按下那個鍵，滑鼠移過去會亮起來。不用再記快捷鍵。只有標示**單一按鍵**的才可以點；`(=[]<>)`（同標題前後篇）、`(/?a)`（搜尋）、`(v/V)`（已讀／未讀）這種一組寫了好幾個鍵的維持原樣的純文字 —— 因為無從得知你想按哪一個，猜錯就送出完全不同的指令（`v` 是標已讀、`V` 是標未讀）。在文章好讀模式下點 `(y)回應`、`(X)推文` 這類會開輸入框的按鈕，畫面會自動切成原生顯示，看得到輸入框。可在「偏好設定 → 滑鼠」關閉。
- **🔙 列表也能點左邊回上一層** — 文章列表與看板列表把滑鼠移到畫面最左邊，同樣會亮出提示帶、指標變成返回箭頭，點一下就回上一層（等同按左方向鍵）。先前只有文章裡有這個手勢。左邊那一小條**永遠是「回上一層」**，不受「防誤觸模式」影響。
- **🛡️ 防誤觸模式（預設開啟）** — 在「偏好設定 → 滑鼠」。開啟時**只有文字內容區才接受點擊**：文章列表要點在標題欄才會開文（點日期或作者欄沒反應），文章裡要點推文的內容文字才會高亮同一個推文者 —— 左側因此空出來給「點一下離開文章」的提示帶，推文區也點得到了。關閉時整條都能點（游標所在列的標示也會跟著變成整條，**可點範圍永遠等於標示的範圍**）。
- **🔆 游標整列提亮（預設開啟）** — 目前游標所在的那一列，**整列文字會變亮**、但**不會蓋上底色**，一眼看得出「按 Enter 會開哪一篇」，又不會有一條色塊把畫面切開。這是還原 PTT 早年那種沒有底色的游標標示（PTT 官方在 2026-08 把這個選項移除了，只留下有底色的「光棒式游標」）。原本就是亮色的字沒有更亮的一階可用，改以整列微微發光來標示。可在「偏好設定 → 一般 → 游標所在列」關閉。
- **🎯 游標底色（預設關閉）** — 上面那條的另一種樣式：游標所在列整列上底色。顏色可自選（15 色），開關與色票都在「偏好設定 → 一般 → 游標所在列」。**兩種樣式可以同時開**（既提亮又上底色）；也可以兩個都關，畫面就完全不標示游標列。**鍵盤操作時也有**（過去只有開著滑鼠瀏覽、且滑鼠停在畫面上才會出現），文章列表、選單都適用，文章列表好讀模式下標的是好讀模式自己的游標；滑鼠停留的開關在「偏好設定 → 滑鼠」。
- **🕶️ 上班模式** — 在「偏好設定 → 本機設定」開啟後，畫面上 BBS 的高彩度顏色（黃推文、藍標題、反白色塊等）全部換成低調的暗灰色系，遠看就像一般的暗色網頁。僅影響顯示、不影響任何功能；此設定只存在本機、**不會**被雲端同步（每台裝置可各自開關）。
- **🐞 Debug 錄製模式** — 在「設定 → 關於」開啟後，主畫面出現錄製按鈕；可把連線收發資料與程式狀態錄成檔案下載，回報難重現的 bug 用。此開關不會被儲存，重新整理後自動關閉；下載前會自動遮蔽已知帳密與 IP。

---

# 🛠️ PTT 三方網頁版連線修復指南

**為什麼會這樣？**
因為 PTT 系統會檢查你是從哪裡連線過來的。如果你的連線來源不是官方網站，就會被擋在門外。

**我們要怎麼解決？**
原理很簡單！我們只需要透過一些小工具，讓瀏覽器「假裝」我們是從 PTT 官方網站 (`https://term.ptt.cc`) 連線過去的，就可以順利進入 PTT 囉！

這裡提供兩種最簡單的解決方法，請擇一使用即可：

---

## 🌟 方法一：安裝擴充套件（強烈推薦，最穩定！）

這個方法是透過安裝一個安全的瀏覽器擴充功能（以 Header Editor Lite為例），來幫我們自動「假裝」成官方連線。只要設定一次，以後就可以像平常一樣順暢上 PTT 了。

### 步驟說明：

1. **安裝 Header Editor Lite 擴充功能**
   請先在你的瀏覽器（火狐為例）安裝 [Header Editor Lite 套件](https://addons.mozilla.org/en-US/firefox/addon/header-editor-lite/) 。
2. **寫入自動設定檔**
   <img width="937" height="1377" alt="圖片" src="https://github.com/user-attachments/assets/abab49cf-6f14-47c7-869d-df74add7c6b4" />

3. **儲存並啟用**
  
4. **完成！**
   現在你可以打開你的第三方 PTT 網站，應該就能順利登入了！

> **💡 技術細節（給想知道原理的人）：**
> 其實這個套件只是在每次連線到 PTT (`wss://ws.ptt.cc/bbs`) 時，自動幫你把身分標籤（Origin）改成官方的 `https://term.ptt.cc` 而已，非常安全。

---

## ⚡ 方法二：使用內建 Proxy（免安裝擴充）

如果你不想安裝任何擴充套件，本站已**內建 Proxy 連線功能**，透過代理伺服器幫你轉發連線，不必安裝、不必另外架站。

### 如何使用：

1. 在畫面上**按右鍵** → 選擇「**偏好設定**」。
2. 切換到「**連線**」分頁。
3. 勾選「**透過 proxy 連線 (重新整理後生效)**」。
4. 下方的「**Proxy 位址**」**留空就會使用內建的公開代理伺服器**（欄位裡淡色顯示的那個位址），直接**重新整理網頁**就會生效。
   - 如果你有自己的 proxy，也可以填自己的：可以是網址（例如 `your-proxy.example.workers.dev`），或完整的 `wsstelnet://主機/路徑`。
   - 填了之後想改回預設，把欄位**整段刪空**即可。

> **⚠️ 注意事項：**
> * 連線是在**開啟網頁的當下**建立的，所以**修改設定後一定要重新整理網頁**才會生效。
> * 透過代理伺服器連線時，連線速度會受到代理伺服器影響，比直連稍微慢一點點是正常的！

---

# 🔗 讓 PTT 文章網址直接在本站開啟

**使用場景：**
你在 Google 搜尋結果、LINE、Discord、書籤裡點到的，通常是 PTT 官方網頁版的文章網址：

```
https://www.ptt.cc/bbs/Browsers/M.1786265274.A.5E3.html
```

點下去只會開到官方網頁版（沒有推文樓層、自動開圖、黑名單這些增強功能）。
裝好下面這條規則後，同一個連結會**自動變成本站的文章連結並直接跳到那篇文章**，
而且如果你已經有一個登入好的分頁（或把本站安裝成應用程式），會直接交給它去跳，不用重新登入。

### 步驟說明：

1. **安裝 Header Editor Lite 擴充功能**（與上面「方法一」同一個套件，已裝過就跳過）
   [Header Editor Lite ](https://addons.mozilla.org/en-US/firefox/addon/header-editor-lite/)
2. **寫入設定檔**
<img width="942" height="1369" alt="圖片" src="https://github.com/user-attachments/assets/4ea3e5e8-b581-4f23-836e-21c0f4d38bf4" />
3. **儲存並啟用**
### 原理

套件用下面這條規則比對你正在開啟的網址（只認**文章**網址，看板列表、搜尋頁等一律不動）：

```
/^https?:\/\/www\.ptt\.cc\/bbs\/([A-Za-z0-9_-]+\/M\.\d+\.A\.[A-Z0-9]+\.html)/
```

命中後把網址改寫成本站的分享連結（把「看板名／檔名」原樣接在 `#` 後面）：

```
https://abccbaandy.github.io/PttChrome/#$1
```

於是：

```
https://www.ptt.cc/bbs/Browsers/M.1786265274.A.5E3.html
        ↓ 套件改寫
https://abccbaandy.github.io/PttChrome/#Browsers/M.1786265274.A.5E3.html
        ↓ 本站的文章分享連結（deep link）功能接手
自動切換到該看板 → 開啟該篇文章
```

也就是說，這條規則只是幫你把網址「翻譯」成本站看得懂的形式，
真正跳轉的是本站原本就有的**文章分享連結（deep link）**功能——你手動把網址複製貼上改成 `#` 形式，效果完全一樣。

> **💡 小提醒：**
> * 如果你是自己部署本站（自架或改用別的網址），請把規則裡的目標網址換成你自己的站台位址。
> * 想暫時看官方網頁版時，在 Requestly 把這條規則關掉即可（連線用的 Origin 規則請保持開啟）。
> * 跳轉需要先登入 PTT；還沒登入的話本站會先把文章記著，等你登入完成再自動跳過去。

---

## 🔐 自動登入／記憶帳密

在「**偏好設定 → 自動登入**」分頁勾選「**開啟網頁時自動登入**」，並在同一頁下方填入帳號、密碼（以及 2FA 密鑰，若有），之後開啟網頁就會自動幫你登入。

### 密碼是怎麼儲存的？

為了安全，密碼**不會長期以明文存在你的電腦裡**，而是交給瀏覽器內建的密碼管理員保管：

- **第一次儲存時，瀏覽器會跳出「是否儲存密碼？」的提示，請務必按「儲存」**，自動登入之後才讀得到密碼。
- 你填入的內容會先暫存在本機，等到下次自動登入時真的從密碼管理員讀回來，才會自動清掉本機那份。所以**設定頁的欄位空白代表「已交給密碼管理員保管」，不是沒存成功**（欄位的提示文字也會說明目前狀態）。
- 要更新帳密時，直接重新填入即可；關閉設定頁時瀏覽器會跳出「更新密碼」提示。
- **Chrome**：使用 Google 密碼管理員，支援最完整。
- **Edge**：目前有個 bug，密碼可以存、但讀不回來（自動登入可能會失效）。
- **其他不支援密碼管理員的瀏覽器**（例如部分 Firefox／Safari）：會退而以明文存在本機**且不會自動清除**，請**只在自己的個人裝置**上使用（設定頁有「清除本機暫存的登入資訊」按鈕）。

### 兩階段驗證 (2FA)

如果你的 PTT 帳號開了兩階段驗證，可以把 TOTP 密鑰填進「**兩階段驗證密鑰**」欄位（16 碼的 A-Z 2-7，或直接貼 PTT 給你的整段 `otpauth://` 網址），登入時就會自動算出當下的 6 位驗證碼填入。

- **留空也完全可用**：自動登入會填完帳號密碼、停在驗證碼畫面，讓你自己輸入 6 碼，不會亂送按鍵。
- **安全取捨**：密鑰會和密碼存在同一個密碼管理員，等於放棄第二道關卡對「密碼管理員被攻破」的保護（對 PTT 端密碼外洩仍然有保護）。密鑰屬於長期憑證，一旦外洩必須重設 2FA 才能作廢。在意的話就留空手動輸入，或在 PTT 端把 2FA 設成「只有新 IP 才驗證」。
- 8 位數的「救援碼」是一次性的，不會自動填。
- 驗證碼連續兩次被拒（密鑰填錯或電腦時間不準）就會停手，把鍵盤交還給你——PTT 給 5 次機會，剩下的留給你手動輸入。

### 注意事項：

- 需要在安全環境下使用（網址是 `localhost` 或 `https://` 的網站）。
- PTT 密碼與 2FA 密鑰**絕對不會上傳雲端**，即使你開啟了「偏好設定雲端同步」也一樣。
- 另外可以設定：偵測到「重複登入」時要不要踢掉其他連線、以及是否自動跳過歡迎畫面（這兩項會雲端同步）。
